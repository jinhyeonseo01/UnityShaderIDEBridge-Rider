# Examples

This folder is reserved for full Unity example projects:

- `Examples/urp-project`
- `Examples/hdrp-project`
- `Examples/builtin-project`

Each project should consume the package via Git UPM or OpenUPM.
